"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const chai_1 = require("chai");
const generation_1 = require("./generation");
describe("Ethers generation edge cases", () => {
    const emptyContract = {
        name: "TestContract",
        functions: {},
        events: {},
        constructor: [{ name: "constructor", inputs: [], outputs: [], stateMutability: "nonpayable" }],
    };
    it("should generate simple factory when no bytecode available", () => {
        chai_1.expect(generation_1.codegenContractFactory(emptyContract, "abi", undefined)).to.match(/export class TestContractFactory \{/);
    });
});
